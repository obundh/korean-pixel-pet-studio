# PixelPet Studio 처음 시작하기

코딩도 터미널도 필요 없습니다. 설치 파일을 받은 뒤 화면의 안내를 따라가면 가로 포즈 시트 한 장을 실제 프레임으로 움직이는 데스크톱 펫으로 만들 수 있습니다.

> 가장 빨리 확인하려면 아래의 **A. 3분 샘플 실행**부터 시작하세요. 실제 포즈가 움직이는 내 펫을 만들려면 **C. 원본 마스코트부터 정식으로 만들기**를 따라가세요. **B. 빠른 모드**는 이미지 한 장만 있을 때의 간단한 시험용입니다.

## 준비물

- Windows 10/11 또는 macOS 컴퓨터
- 공개하거나 사용할 권한이 있는 마스코트 이미지
- AI로 새 픽셀 이미지를 만들 경우 GPT 또는 Gemini처럼 이미지 생성과 이미지 첨부를 지원하는 서비스
- 프로젝트를 저장할 폴더 하나

PixelPet Studio 설치와 샘플 실행에는 Node.js, Git, 명령 프롬프트가 필요하지 않습니다.

## 1. 앱 받기

공식 [GitHub Releases](https://github.com/obundh/korean-pixel-pet-studio/releases/latest)에서 운영체제에 맞는 파일을 받으세요.

| 내 컴퓨터 | 받을 파일 | 추천 상황 |
|---|---|---|
| Windows 10/11 64비트 | `PixelPet-Studio-Setup-...-x64.exe` | 계속 설치해 두고 사용 |
| Windows 10/11 64비트 | `PixelPet-Studio-Portable-...-x64.exe` | 설치 없이 먼저 시험 |
| Apple Silicon Mac | `PixelPet-Studio-...-arm64.dmg` | Apple M 계열 칩을 사용하는 Mac |
| Intel Mac | `PixelPet-Studio-...-x64.dmg` | Intel 프로세서 Mac |

파일 이름의 버전 번호는 릴리스에 따라 달라질 수 있습니다. `arm64`와 `x64`를 구분하는 방법과 보안 경고 해결법은 다음 문서에 있습니다.

- [Windows 설치](docs/beginner/INSTALL_WINDOWS.md)
- [macOS 설치](docs/beginner/INSTALL_MACOS.md)

보안상 이 저장소의 공식 Releases 이외에서 다시 올린 설치 파일은 사용하지 마세요.

## A. 3분 샘플 실행

이 경로는 앱이 내 컴퓨터에서 제대로 실행되는지 먼저 확인하는 과정입니다.

1. 저장소의 `examples/beginner-pets/` 폴더를 엽니다.
2. [예제 안내](examples/README.md)의 공개 오리지널 샘플 표에서 펫 하나를 고릅니다.
3. 그 폴더 안의 `.pixelpet` 파일을 누릅니다. GitHub 파일 화면이 열리면 오른쪽 위 **Download raw file** 버튼으로 컴퓨터에 저장합니다. 화면의 긴 JSON 글자를 복사하지 마세요.
4. PixelPet Studio를 실행합니다.
5. 앱 위쪽의 **불러오기**를 누릅니다.
6. 방금 저장한 `.pixelpet` 파일을 선택합니다.
7. 왼쪽의 **움직임 확인**을 눌러 서로 다른 대기 포즈 4장이 4 FPS로 재생되는지 확인합니다.
8. 왼쪽의 **펫 완성**을 누른 뒤 **데스크톱에서 실행**을 누릅니다.

화면 위에 투명 배경의 작은 펫이 나타나면 설치와 샘플 불러오기가 끝난 것입니다. 앱으로 돌아가 **데스크톱 펫 종료**를 누르면 펫 창이 닫힙니다.

샘플 파일의 구성과 공개 범위는 [예제 안내](examples/README.md)에서 확인할 수 있습니다.

## B. 이미지 한 장으로 빠르게 시험하기

이미 픽셀화된 캐릭터 이미지가 한 장 있다면 이 방법이 가장 쉽습니다.

빠른 만들기는 **생성 가이드**에서 현재 선택한 스타일의 캔버스 크기를 사용합니다. 아무것도 바꾸지 않으면 기본 64×64 스타일을 사용하고, 일부 스타일을 고르면 96×96으로 만들 수 있습니다.

1. 앱의 **배경 제거** 단계에서 **이미지 한 장으로 자동 완성** 카드와 **추천** 표시를 찾습니다.
2. 카드 안의 **이미지 한 장 고르기**를 누릅니다.
3. PNG, JPG 또는 WebP 이미지 한 장을 선택합니다. 파일은 20MB·전체 4MP 이하이고 가로와 세로가 각각 4096px 이하여야 합니다.
4. 앱이 기기 안에서 배경과 남은 단색 경계색을 제거하고 선택한 스타일의 캔버스 크기에 맞춥니다. 기본 예시는 64×64이고 일부 스타일은 96×96입니다.
5. 같은 그림을 정수 픽셀만큼 이동한 대기(`idle`) 프레임 4장이 4 FPS로 준비됩니다.
6. 결과 화면에서 테두리와 잘린 부분이 없는지 확인합니다.
7. **움직임 확인**에서 속도와 크기를 조절합니다.
8. **펫 완성 → 프로젝트 저장**으로 `.pixelpet` 파일을 저장합니다.
9. **데스크톱에서 실행**을 눌러 사용합니다.

빠른 만들기는 새 포즈를 AI로 그리는 기능이 아닙니다. 한 장의 픽셀 이미지를 배경 제거한 뒤 선택한 스타일 캔버스에 정규화하고, `0px → 위로 1px → 위로 1px → 0px`로만 이동합니다. 몸 전체를 늘이거나 찌그러뜨리지 않는 **무왜곡 bob**이지만, 팔·다리·표정·장식이 변하는 진짜 포즈 애니메이션은 아닙니다.

실제로 숨 쉬거나 꼬리·귀가 움직이는 펫은 아래의 정식 제작 경로를 사용하세요. 자세한 버튼 설명은 [앱 따라 하기](docs/beginner/APP_WALKTHROUGH.md)에 있습니다.

## C. 원본 마스코트부터 정식으로 만들기

### 1단계: 작업 폴더 만들기

탐색기 또는 Finder에서 아래처럼 폴더를 만들어 두면 프레임 순서가 덜 헷갈립니다.

```text
내-픽셀-펫/
├── 01-원본/
├── 02-기준-픽셀/
├── 03-idle-포즈-시트/
├── 04-walk-걷기/
└── 05-프로젝트/
```

### 2단계: 기준 픽셀 이미지 만들기

1. PixelPet Studio의 **생성 가이드**에서 스타일 하나를 선택합니다.
2. **선택한 스타일 레퍼런스 저장**을 눌러 스타일 이미지를 저장합니다.
3. 원본 마스코트와 스타일 레퍼런스를 GPT 또는 Gemini에 순서대로 첨부합니다.
4. 기준 픽셀 마스터용 프롬프트를 붙여 넣습니다.
5. 결과가 같은 캐릭터로 보이는지 확인하고 `02-기준-픽셀` 폴더에 저장합니다.

AI 서비스에서 이미지를 첨부하는 순서와 실패 결과를 고치는 방법은 [AI로 이미지 만들기](docs/beginner/CREATE_WITH_AI.md)에 있습니다.

### 3단계: 우선 대기 포즈 4칸 시트 만들기

첫 성공에는 28장을 전부 만들 필요가 없습니다. `대기` 포즈 4개를 한 줄로 나란히 만든 시트 한 장이면 됩니다.

1. 앱의 **생성 가이드 → 동작과 프레임 순서**에서 **대기**를 선택합니다.
2. F01부터 F04까지 포즈 레퍼런스를 각각 저장합니다.
3. AI 서비스에 다음 순서로 이미지를 첨부합니다.
   1. 승인한 기준 픽셀 마스터
   2. 같은 스타일 레퍼런스
   3. F01~F04 포즈 레퍼런스
   4. 원본 마스코트는 가려진 특징을 확인할 때만 추가
4. “왼쪽부터 F01, F02, F03, F04인 같은 너비의 4칸 가로 시트”를 요청합니다.
5. 칸 사이 선, 글자, 번호, 여백 띠가 없는지 확인합니다.
6. 결과를 `idle-sheet.png`처럼 한 파일로 `03-idle-포즈-시트` 폴더에 저장합니다.

시트 전체가 한 줄이어야 하며, 가로 길이는 4로 정확히 나누어져야 합니다. 앱의 전체 이미지 제한은 20MB·4MP·한 변 4096px입니다. 4096×1024 시트는 4MP를 조금 넘으므로 그대로 넣을 수 없습니다. 각 칸을 960×960으로 줄여 전체를 3840×960으로 만들면 제한 안에 들어옵니다. 픽셀아트를 줄일 때는 nearest-neighbor 보간을 사용하세요.

### 4단계: 앱에서 나누고 한 번에 정리하기

1. 왼쪽 **프레임 가져오기**를 누릅니다.
2. 위쪽 동작 탭에서 **대기**를 선택합니다.
3. **포즈 시트 한 장 가져오기**를 누르고 `idle-sheet.png`를 선택합니다.
4. 앱이 시트를 왼쪽부터 정확히 4등분해 01~04 슬롯에 넣었는지 확인합니다.
5. **이 동작 한 번에 정리**를 누릅니다.
6. 앱이 네 칸의 배경을 로컬에서 제거하고, 머리·몸통처럼 넓고 안정적인 가로 행을 기준으로 작은 좌우 드리프트만 제한적으로 보정한 뒤 시트의 세로 위치와 발 기준선을 유지하면서 모든 포즈에 같은 공통 캔버스와 배율을 적용할 때까지 기다립니다.
7. “공통 좌표 정리 완료”가 보이고 네 프레임에 투명 배경이 적용됐는지 확인합니다.

시트 분할, 배경 제거와 정렬 과정에서 이미지는 프로그램 밖으로 전송되지 않습니다. 첫 처리에서는 앱에 포함된 로컬 모델을 메모리에 올리느라 조금 더 걸릴 수 있습니다.

### 5단계: 움직임 확인하고 저장하기

1. **움직임 확인하기**를 누릅니다.
2. 대기 동작은 일괄 정리 후 기본값인 **4 FPS**, 화면 배율 3×로 먼저 봅니다.
3. 얼굴이나 발 위치가 프레임마다 튀지 않는지, 팔·꼬리·표정 같은 실제 포즈가 변하는지 봅니다.
4. **펫 완성하기 → 프로젝트 저장**을 누릅니다.
5. `05-프로젝트` 폴더에 `.pixelpet` 파일로 저장합니다.
6. **데스크톱에서 실행**을 누릅니다.

여기까지 성공한 뒤 걷기 8칸, 점프 6칸, 잠자기 4칸, 반응 6칸 가로 시트를 같은 방식으로 추가하면 됩니다. 동작 탭의 슬롯 수에 맞춰 시트 가로를 똑같이 나누어야 합니다.

## 저장 파일은 무엇이 다른가요?

| 버튼 | 결과 | 언제 사용하나요? |
|---|---|---|
| **프로젝트 저장** | `.pixelpet` | 나중에 다시 열어 계속 편집할 때. 초보자에게 가장 중요합니다. |
| **PNG 내보내기** | 스프라이트시트 PNG | 게임 엔진이나 웹 프로젝트에 넣을 때 |
| **JSON 내보내기** | 프레임 데이터가 포함된 JSON | 개발 도구와 연결할 때. 처음에는 사용하지 않아도 됩니다. |

## 막혔다면

- 버튼 위치가 헷갈림: [앱 따라 하기](docs/beginner/APP_WALKTHROUGH.md)
- AI 결과가 자꾸 달라짐: [AI로 이미지 만들기](docs/beginner/CREATE_WITH_AI.md)
- 설치 또는 배경 제거 오류: [문제해결](docs/beginner/TROUBLESHOOTING.md)
- 빠뜨린 것이 있는지 확인: [완성 체크리스트](docs/beginner/CHECKLIST.md)
- 프레임, FPS 같은 단어가 어려움: [쉬운 용어 설명](docs/beginner/GLOSSARY.md)

## 권리와 개인정보

- 기관 마스코트, 로고, 브랜드 이미지는 사용·변형·공개 권한을 먼저 확인하세요.
- 배경 제거는 기기 안에서 실행되지만, GPT 또는 Gemini에 올린 이미지에는 해당 서비스의 데이터 정책이 적용됩니다.
- RRA 파비는 [국립전파연구원 공식 마스코트 페이지](https://www.rra.go.kr/ko/intro/character.do)에 확인 가능한 KOGL 표시가 없어 이 공개 저장소에 원본이나 파생 이미지를 넣지 않습니다. [로컬 전용 recipe](examples/beginner-pets/rra-pavi/README.md)는 공식 링크와 제작 순서만 제공하며, 공개·배포 전 권리자의 사전 허락이 필요합니다.
